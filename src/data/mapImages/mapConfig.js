const mapConfig = {
  intro: {
    // Gestos
    dragPan: true,
    scrollZoom: true,
    // Zoom
    initialZoom: 7.8,
    initialBearing: -90,
    shadow: false,
    minZoom: 7.8,
    maxZoom: 9,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 1,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-78.908544263897, -0.02089838124, -72.289352185258, 12.879198862123],
    boundsPadding: { top: -0.25, bottom: -0.25, left: 0.35, right: 0.35 },
  },
  encuadres: {
    // Gestos
    dragPan: true,
    scrollZoom: true,
    // Zoom
    initialZoom: 6.06,
    initialBearing: -90,
    minZoom: 2,
    maxZoom: 12,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-78.908544263897, -0.02089838124, -71.289352185258, 12.879198862123],
  },
  unRioCaucaMuchosMundos: {
    // Gestos
    dragPan: true,
    scrollZoom: true,
    // Zoom
    initialBearing: -90,
    initialZoom: 6.5,
    minZoom: 5,
    maxZoom: 10,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 1,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-78.908544263897, -0.02089838124, -72.289352185258, 12.879198862123],
    boundsPadding: { top: -0.25, bottom: -0.25, left: 0.35, right: 0.35 },
  },
  bredunco: {
    // Gestos
    dragPan: true,
    scrollZoom: true,
    // Zoom
    initialZoom: 7.1,
    initialBearing: -90,
    minZoom: 7.1,
    maxZoom: 9.5,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-78.908544263897, -0.02089838124, -72.289352185258, 12.879198862123],
    boundsPadding: { top: -0.25, bottom: -0.25, left: 0.35, right: 0.35 },
  },
  fomasDelPaisaje: {
    // Gestos
    dragPan: true,
    scrollZoom: true,
    // Zoom
    initialBearing: -90,
    initialZoom: 7.0,
    minZoom: 5,
    maxZoom: 9.5,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 1,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-78.908544263897, -0.02089838124, -72.289352185258, 12.739198862123],
    boundsPadding: { top: -0.25, bottom: -0.25, left: 0.35, right: 0.35 },
  },
  ecosistemas: {
    // Gestos
    dragPan: true,
    scrollZoom: true,
    // Zoom
    initialBearing: -90,
    minZoom: 5,
    maxZoom: 9.5,
    // Street view
    minZoom: 5,
    streetViewEnabled: true,
    debugMapOpacity: 1,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-77.718, 1.94, -75.2, 5.9],
    boundsPadding: { top: -0.25, bottom: -0.25, left: 0.35, right: 0.35 },
  },
  tejidosDelAgua: {
    // Gestos
    dragPan: true,
    scrollZoom: true,
    // Zoom
    initialBearing: -90,
    initialZoom: 10.5,
    minZoom: 5,
    maxZoom: 10.5,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.3,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-77.718, 1.94, -75.2, 5.9],
    boundsPadding: { top: -0.25, bottom: -0.25, left: 0.35, right: 0.35 },
  },

  // CAP2
  TNATransformadoras: {
    // Gestos
    dragPan: true,
    scrollZoom: true,
    // Zoom
    initialZoom: 8,
    initialBearing: -90,
    minZoom: 8,
    maxZoom: 12,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-77.548544263897, 1.87, -75.249352185258, 5.879198862123],
    boundsPadding: { top: -0.25, bottom: -0.25, left: 0.35, right: 0.35 },
  },

  ASuarez: {
    // Gestos
    dragPan: true,
    scrollZoom: true,
    // Zoom
    minZoom: 6,
    maxZoom: 11,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-76.772, 2.759, -76.635, 3.0055],

  },

  AVillaRica: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    // Zoom
    maxZoom: 15,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-76.55, 2.975, -76.28, 3.465136891448],
  },

  VDOrienteCali: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    // Zoom
    maxZoom: 15,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-76.536968208220, 3.349, -76.458, 3.489136891448],
  },

  MOrienteCali: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    shadow: false,
    // Zoom
    maxZoom: 15,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-79.8, 3.5, -78.2, 5.5],
  },

  MVillaRica: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    shadow: false,
    // Zoom
    maxZoom: 15,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-77.0, 2.5, -76.0, 3.7],
  },

  MSuarez: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    shadow: false,
    // Zoom
    maxZoom: 15,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-76.8, 2.2, -75.8, 3.4],
  },


  introduccionCap3: {
    // Gestos
    dragPan: true,
    scrollZoom: true,
    // Zoom
    maxZoom: 11,
    initialZoom: 8.8,
    minZoom: 8.8,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-78.0, 1.8, -76.5, 3.5],
  },

  monocultivo: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    shadow: false,
    // Zoom
    maxZoom: 11,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-77.5, 2.7, -76.3, 4.0],
  },

  nosEncharcaronElRio: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    shadow: false,
    // Zoom
    maxZoom: 15,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-77.3, 2.3, -76.2, 3.5],
  },
  humedalesCap3: {
    // Gestos
    dragPan: true,
    scrollZoom: true,
    shadow: false,
    // Zoom
    maxZoom: 11,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-78.0, 2.5, -76.5, 4.5],
  },

  arcilla: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    shadow: false,
    // Zoom
    maxZoom: 18,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-77.0, 2.5, -75.8, 3.8],
  },

  caliDeseca: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    shadow: false,
    // Zoom
    maxZoom: 15,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-77.3, 2.7, -76.0, 4.0],
  },

  introduccionCap4: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    shadow: false,
    // Zoom
    initialZoom: 7.0,
    maxZoom: 15.4,
    minZoom: 7.0,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-77.5, 2.5, -76.0, 4.0],
  },

  asoyoge: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    shadow: false,
    // Zoom
    initialZoom: 17.4,
    maxZoom: 17.4,
    showCoordinates: false,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-76.687, 2.930, -76.679, 2.942],
  },
  elBuhido: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    shadow: false,
    // Zoom
    initialZoom: 17.4,
    maxZoom: 17.4,
    showCoordinates: false,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-76.686, 2.938, -76.678, 2.946],
  },
  bosqueComestible: {
    // Gestos
    dragPan: true,
    scrollZoom: true,
    shadow: false,
    // Zoom
    initialZoom: 17.4,
    minZoom: 17.4,
    maxZoom: 17.4,
    showCoordinates: false,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-76.497, 3.439, -76.488, 3.446],
  },
  losBajios: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    shadow: false,
    // Zoom
    initialZoom: 18.4,
    maxZoom: 18.4,
    showCoordinates: false,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-76.443, 3.190, -76.438, 3.195],
  },
  elPaso: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    shadow: false,
    // Zoom
    initialZoom: 18.4,
    maxZoom: 18.4,
    showCoordinates: false,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-76.675, 2.954, -76.670, 2.960],
  },

  lasMercedes: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    shadow: false,
    // Zoom
    initialZoom: 18.4,
    maxZoom: 18.4,
    showCoordinates: false,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-76.689, 2.928, -76.683, 2.934],
  },

  laVirginia: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    shadow: false,
    // Zoom
    initialZoom: 18.4,
    maxZoom: 18.4,
    showCoordinates: false,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-76.293, 3.222, -76.287, 3.228],
  },

  centroAgropecuario: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    shadow: false,
    // Zoom
    initialZoom: 18.4,
    maxZoom: 18.4,
    showCoordinates: false,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-76.434, 3.184, -76.428, 3.190],
  },

  laCaicedo: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    shadow: false,
    // Zoom
    initialZoom: 18.4,
    maxZoom: 18.4,
    showCoordinates: false,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-76.431, 3.182, -76.425, 3.188],
  },


  problematicas: {
    // Gestos
    dragPan: false,
    scrollZoom: true,
    shadow: false,
    // Zoom
    initialZoom: 18.4,
    maxZoom: 18.4,
    initialBearing: -90,
    // Street view
    streetViewEnabled: true,
    debugMapOpacity: 0.5,
    // Bounds
    useTransformConstrain: true,
    viewportMaxBounds: [-76.505, 3.433, -76.498, 3.441],
  },

};

export default mapConfig;
